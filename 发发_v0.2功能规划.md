# 发发桌宠 v0.2 功能规划

更新日期：2026-08（v0.2 开发进行中）

本文件记录 v0.2 的完整范围与实施顺序（经用户确认：功能清单 1-14 + 16 全部纳入）。Live2D 仅预留接口，不接 SDK；外部服务采用「预设自动配置 + 只填 Key 即可测试」的交互方式。

## 阶段 A · 地基与语音输入 ✅ 全部完成

1. ✅ 配置 v3 迁移 + Provider 预设注册表（TTS/LLM/ASR：百炼、DeepSeek、自定义；预设自动填接口参数，只需填 Key 即可测试）。
2. ✅ 麦克风声音检测：设备选择、实时音量测试、大叫阈值/灵敏度/反应冷却自主调节；超阈值触发大叫（emo_shout），持续嘈杂触发捂耳朵（act_cover_ears）。
3. ✅ ASR 语音转文字：百炼 paraformer-realtime-v2 流式识别（渲染层 16kHz PCM 降采样 → 主进程 WS 会话），离线降级。
4. ✅ AI 输入源三选一（仅语音转写 / 仅弹幕 / 全部）+ API 测试对话框（LLM 对话、TTS 试听、ASR 连接测试、麦克风音量表）。
5. ✅ 设置页语音与 AI 面板；TTS 回复范围四选（仅语音 / 仅弹幕 / 全部 / 关闭）+ 从 API 读取音色列表（保留自定义音色回显）。

## 阶段 B · 语音生态 ✅ 全部完成

6. ✅ 主播语音指令：转写文本复用现有指令解析（说「发发跳舞」= 弹幕指令）。
7. ✅ 语音唤醒词：「发发」唤醒后 15 秒内处理语音指令与对话（可关闭）。
8. ✅ 弹幕喊名字免前缀：弹幕出现「发发」即回应（8 秒全局节流，可关闭）。

## 阶段 C · 陪伴与养成 ✅ 全部完成

9. ✅ 勿扰时段（跨午夜区间，静音 TTS/空闲/大叫反应）+ 设置页开关。
10. ✅ 整点报时 + 定时提醒（自定义 HH:MM 提醒编辑器）。
11. ✅ 贴边收纳（拖到屏幕边缘缩成小图标，双击恢复，可关闭）+ 分部位抚摸（头/肚子/身体不同反应）。
12. ✅ 养成与关系系统：亲密度/每日互动/关系阶段（初见→家人）/短期情绪衰减/成长日记（最近 30 天）/下播小结；关系面板 + 重置入口。

## 阶段 D · 直播能力

13. ✅ 直播事件快照（≤200 条时间线）与来源标记 → 直播控制台核心：暂停/恢复、立即重连、按来源清队列（设置页 UI 实现中）。
14. ✅ 礼物分级反馈（普通/中等/大额/豪华）+ 点歌曲目气泡（currentTrack）。
15. ✅ 弹幕小游戏（猜拳、抽签）。
16. ✅ 直播内容安全：四类事件独立开关、敏感词过滤（原文不进 LLM/TTS）、TTS 回复范围（设置页 UI 实现中）。

## 阶段 E · 收尾

17. ✅ 掌声彩蛋（音量包络启发式 + 接口预留，ML 后续接入）。
18. ✅ 素材重做：27 个程序化连续动画素材全部集成（含新动作 emo_shout/act_cover_ears），release 素材 QA 40/40 通过。
19. ⬜ 全量回归（check + 196 项测试 + 冒烟）与测试版打包（test-build-v020，electron-builder --dir 不生成安装器）。

## 备注

- 百炼 ASR 实时 WS 协议细节以官方文档为准，测试对话框会展示具体错误便于微调。
- 音色列表接口失败时回退内置列表并允许手动输入。
- Live2D 接口已预留（PetRenderer 契约 + Live2DPetRenderer 空适配器），模型与 SDK 后续接入。

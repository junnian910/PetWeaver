# 发发桌宠 v0.1 完成说明

更新日期：2026-08-14  
当前发布版本：v0.1.2

## 1. 桌宠窗口与基础交互

- Windows 透明、无边框、置顶桌宠窗口。
- 托盘菜单、设置窗口、窗口位置保存与恢复。
- 支持单击、双击、抚摸、拖动、抛掷和右键菜单。
- 支持点击穿透及快捷键切换。
- 拖动期间保持当前画面、角色比例和缩放不变，不再切换成放大的抓取图片。
- 抛掷具有 airborne、impact、landed 阶段反馈；普通拖动不会误触发抛掷状态。
- 多显示器工作区边界、屏幕缝隙和无效坐标已有保护。

## 2. 动作与视频播放

- 已建立统一动作注册表，包含 38 个策划动作和 3 个扩展运行状态。
- 38 个策划动作均有独立视频路径，不再借用其他动作的视频。
- 正式动作视频统一为 384×416、60 FPS、VP9 Alpha 透明 WebM。
- 新生成动作使用连续缓动、重心位移、旋转、缩放和姿势间短时融合，不再按大段时间硬切静态图片。
- 跳跃、舞蹈、翻滚、旋转、喷嚏等动作具有各自的运动轨迹。
- 播放器使用两个视频图层，动作切换时进行 150ms 交叉淡化，避免黑帧和瞬时跳变。
- 视频加载失败时自动回退到呼吸待机，不会使桌宠消失。
- 设置页从注册表生成动作测试入口，可逐一测试全部动作。
- 支持移动循环和 TTS 说话循环；移动方向可镜像。

## 3. 动作调度与语音

- 默认循环播放呼吸待机，并支持眨眼、发呆、随机动作、情绪、命令、礼物和特殊动作。
- 睡眠保持循环，直到唤醒或更高优先级事件结束。
- 高优先级中断采用原子顺序：当前动作结束、紧急动作、原队列动作。
- TTS 在动作实际开始后进入说话循环，播放结束后恢复相应动作或待机。
- 支持阿里云百炼 TTS、DeepSeek OpenAI 兼容接口和离线降级台词。

## 4. 直播与外部服务

- 支持 B 站弹幕、礼物、SC、上舰和连接状态事件。
- 支持命令别名、主播权限、冷却、限流、去重和断线重连。
- 支持抽奖和可配置 HTTP/WebSocket 点歌适配器。
- 外部服务未配置时可离线启动和测试。
- API Key 使用 Electron safeStorage 保存，日志进行脱敏处理。

## 5. 配置、接口与兼容性

- 配置支持版本迁移、嵌套 music 配置合并和命令校验，不覆盖用户已有命令。
- 已定义统一 PetRenderer 契约，并提供 VideoPetRenderer 实现。
- 已提供 Live2DPetRenderer 空适配器；没有可用模型和 SDK 时明确回退视频播放器。
- 渲染页启用 Electron sandbox、context isolation 和 preload 白名单 IPC。

## 6. 代码封装（2026-08 重构）

- 主进程入口 src/main/main.js 已拆分为职责单一的模块：pet-window.js（桌宠窗口生命周期与位置持久化）、pet-physics.js（拖动/抛掷/复位物理）、settings-window.js（设置窗口）、menus.js（托盘与右键菜单）、ipc.js（IPC 白名单与全局快捷键）、window-utils.js（页面加载）。
- 拖动与抛掷的全部窗口物理逻辑集中在 PetPhysics，通过注入 window/screen/config/controller 依赖，可在无 Electron 环境下单元测试。
- 修复了直播适配器双重重连（改用 LiveWS，由适配器独占指数退避）、礼物连击去重按（用户×礼物）聚合、拖拽中断抛掷后渲染页卡在 airborne 阶段等缺陷。

## 7. 质量验证

- 动作注册表检查：38 个策划动作和 3 个扩展状态，ID 与资源路径唯一。
- 视频检查：分辨率、帧率、时长、VP9 Alpha、运动范围和首尾归位。
- 自动化测试覆盖调度、播放器、拖动、抛掷、配置迁移、事件去重、命令、TTS 和视频 QA。
- v0.1.2 最终验证：JavaScript 检查通过，21 个测试文件共 100 项测试通过，Web 生产构建通过。

## 8. 运行与验证

```powershell
pnpm dev
pnpm run check
pnpm test
pnpm run build:web
pnpm run validate:actions:release
```


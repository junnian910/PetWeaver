# 发发桌宠项目交接记录

更新时间：2026-08-13

这份文件是换新对话窗口后的项目入口。新窗口不会可靠地继承当前对话的全部上下文；打开项目后先阅读本文件，再查看 `README.md`、`发发桌宠设计方案_v1.0.md` 和 `发发_v0.2功能规划.md`。

## 当前项目位置与运行方式

- 项目目录：`C:\Users\zzx17\Desktop\fafa`
- 技术栈：Electron + Vite + JavaScript；主进程、preload、桌宠渲染页、设置页分离。
- 开发运行：`pnpm dev`
- 快速检查：`pnpm run check`
- 单元测试：`pnpm test`
- Web 构建：`pnpm run build:web`
- 当前测试版：`test-build-current\win-unpacked\发发.exe`
- 上一个测试版：`test-build-final\win-unpacked\发发.exe`
- 正式安装包目录仍保留在 `release*`；当前阶段不要生成安装器，先用测试版验证。

## 已完成的功能

### 桌宠窗口与交互

- Windows 透明、无边框、置顶桌宠窗口。
- 窗口位置保存、恢复、多显示器虚拟桌面边界计算、托盘菜单、设置窗口、快捷键 `Ctrl+Alt+F`。
- 点击穿透及恢复、右键菜单、单击/双击、抚摸、拖动、甩飞、空中/碰撞/落地阶段反馈。
- 拖动已改为主进程按屏幕坐标定时跟随，renderer 不再通过每一帧 IPC 传位置。
- 角色可通过透明边距贴近屏幕边缘；边界按角色可见内容的 inset 计算，不再只按整个透明窗口矩形限制。
- 本次已加固：边界计算会过滤坏的显示器数据，所有坐标都会收敛为有限整数；鼠标坐标读取失败、窗口已经销毁或 `setPosition` 抛错时会清理拖拽定时器，避免边缘异常继续冒泡。

### 角色显示与动作

- 正式角色基准是 `public/assets/fafa-pet-final.png`。
- 已接入生成的视频动作层，当前动作映射包括待机、挥手、唱歌、睡觉、起床、跳跃、失败和等待等状态。
- 有 Pixi/程序化 fallback 框架，视频加载失败时仍能显示角色和基础动作。
- 已有呼吸、眨眼、轻微摇摆、点击反馈、表情映射、气泡文本。
- 已实现甩飞时的抓起、空中、碰撞、完全落地阶段；落地表情只在真正落地时触发，减少了地面抖动和重复触发。

### 设置与服务骨架

- 设置页已覆盖房间号、主播 UID、缩放、音量、置顶、点击穿透、开机启动、直播开关、TTS、LLM、点歌适配器、指令表和诊断状态。
- 设置页有动作测试入口，可直接测试动作 ID、台词和持续时间。
- B站事件统一为弹幕、礼物、SC、上舰和连接状态；有匿名连接、模拟事件、去重、冷却、限流和重连骨架。
- 已有抽奖、指令别名、主播权限、动作优先级/队列/打断规则。
- 已有阿里云百炼 TTS、DeepSeek OpenAI 兼容接口、离线 TTS/LLM 降级、可配置 HTTP/WebSocket 点歌 Provider；外部服务未配置时可以离线启动。
- API Key 通过 Electron safeStorage 保存，日志脱敏和配置迁移测试已存在。

## 已验证结果

- `pnpm run check`：通过。
- `pnpm test`：10 个测试文件、33 个测试通过。
- 已覆盖：边界计算、无效坐标保护、拖拽手势、甩飞物理、动作调度、动作映射、指令别名、冷却限流、权限、抽奖、日志脱敏。
- 已有真实 Windows 鼠标拖动验证：从左上区域拖到右下区域可以连续移动。

## 之前出现过的问题与处理结果

1. 下载后白框/看不到图片：原因包括透明窗口初始状态、视频尚未 ready、fallback 没有接管。已加入加载状态、视频 ready/error 处理和 fallback。
2. 动作切换时突然变大变小：不同视频的透明画布和主体占比不同，不能只切换 CSS transform。现在使用视频状态映射和逐状态校正，并使用 `useContentSize: true` 减少 Windows DPI 导致的外框变化。
3. 角色一直向左平移：动作素材中心和透明画布中心不一致。已做统一布局和素材校正；后续新增视频仍必须先检查 alpha 内容边界和中心点。
4. 拖动范围越来越小：错误地把动态 DOM 尺寸/透明窗口尺寸混在边界缓存里。现在拖动使用固定窗口 `getBounds()`、启动时建立一次 inset，并由主进程按虚拟桌面边界限制。
5. 曾经尝试使用 `lostpointercapture` 结束拖拽：这是错误方案。窗口移动到屏幕边缘时浏览器可能触发 pointer capture 丢失，导致拖拽提前结束。不要重新加入这个处理；当前使用 pointerup/pointercancel 加主进程拖拽定时器清理。
6. 曾经按整个窗口矩形限制移动：透明边距会让角色看起来不能到边缘。必须使用 `getPetMovementInsets()`，不能恢复成简单的 `desktop.right - window.width`。
7. 曾经把“页面可交互”误判成“媒体已加载”：运行验证必须等待 video ready 或 fallback ready，不要只看页面标题/DOM 存在。
8. 甩飞落地后频繁抖动：物理层原来在接近地面时反复触发。现在降低反弹并在真正落地后停止定时器，只发送一次 landed 阶段。

## 下次绝对不要重复的判断

- 不要重新改回 renderer 每帧 IPC 拖动；位置跟随必须留在主进程。
- 不要对 `screen.getCursorScreenPoint()`、显示器 bounds、`getBounds()` 的结果不做有限数值检查。
- 不要把 `NaN`、`Infinity`、小数边界直接传给 Electron 的 `setPosition`。
- 不要用 `lostpointercapture` 作为拖拽结束条件。
- 不要把 CSS `getBoundingClientRect()` 当成 Electron 窗口物理尺寸。
- 不要用全窗口尺寸代替角色可见内容 inset。
- 不要为了测试清理旧的正式 release；测试目录只保留 `test-build-current` 和 `test-build-final`。
- 不要在用户确认测试前运行 `pnpm build` 生成 NSIS 安装器。

## 下一步建议

1. 用 `test-build-current\win-unpacked\发发.exe` 实测四个角、四条边和多显示器拖动，确认本次边缘异常消失。
2. 如果仍报错，记录完整错误文本、触发位置、是否为主屏/副屏、是否开启点击穿透；不要只描述“边缘报错”。
3. 等拖动和动作测试稳定后，再继续复杂服务联调和最终安装包构建。

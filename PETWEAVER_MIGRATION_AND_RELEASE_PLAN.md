# PetWeaver 迁移与历史版本发布计划

> 当前项目：`Desktop-pet-fafa` / 发发桌宠
>
> 目标项目名：**PetWeaver（宠织引擎）**
>
> 计划起始日：2026-09-16
> 本文用途：记录项目现状、历史版本发布顺序、迁移原则与最终验收标准。

## 1. 项目定位

目前的 `fafa` 已经不只是一个单独的桌宠程序。它包含桌面窗口、托盘、快捷键、拖拽与抛掷物理、动作调度、直播平台适配、LLM/TTS/ASR、记忆、音乐、视频渲染，以及 Live2D/VTube Studio 接口等能力。

下一阶段不应只继续添加“发发专属功能”，而是把这些能力整理为一个可复用的桌宠底层框架：

- 开发者可以制作自己的角色包，而不必修改底层代码。
- 角色的素材、动作、人格、提示词和能力通过清单与配置接入。
- 平台适配器、AI 服务和渲染器能够按需组合、替换。
- `发发` 从硬编码主程序变成 PetWeaver 的官方示例角色。
- 核心框架可以独立测试、构建、发布和二次开发。

## 2. Codex for Open Source 申请判断

### 2.1 中文是否影响申请

中文不是官方列出的淘汰条件，申请页面本身也提供中文版本。真正的风险不是“用了中文”，而是国际审核者无法快速理解项目价值、安装方式、维护状态和生态影响。

因此采用以下文档策略：

- 根目录 `README.md` 以英文为主，并在顶部链接 `README.zh-CN.md`。
- 中文说明保留完整，不牺牲现有用户体验。
- 项目定位、安装、最小示例、架构、许可证和贡献方式必须有英文版本。
- Release Notes 至少提供简洁英文摘要，可附完整中文说明。
- 申请表中的核心回答优先用简洁英文填写，必要时附中文项目名称。

### 2.2 项目是否必须是“自动化项目”

不需要。官方项目面向活跃开源项目的核心维护者，以及被广泛使用或对软件生态有明确价值的公开项目。官方所说的 PR 审查、维护自动化和发布工作流，是 Codex/API 额度的使用场景，不是要求被申请项目本身必须属于自动化工具。

PetWeaver 的合理申请叙事应是：

> PetWeaver 是一个模块化的开源桌面角色运行时与创作框架。它把角色资源、动作、渲染器、直播平台适配器和 AI 提供方解耦，使创作者能够在不修改底层代码的情况下构建自己的桌宠。

不要为了申请而把产品伪装成自动化项目。可以真实地把 Codex/API 用在维护工作中，例如：

- 自动检查第三方角色包的清单、资源路径、权限声明和兼容性。
- 协助处理 Issue、复现问题、分类回归和审查 Pull Request。
- 为多个 renderer/provider/adapter 运行测试矩阵。
- 生成发布候选检查结果、变更摘要和迁移提示。
- 对公开角色包和插件进行基础安全检查。

### 2.3 当前是否适合直接申请

目前不建议立即提交。当前项目还没有形成可审核的干净公开仓库，也缺少能够证明采用度的星标、下载量、外部贡献或生态依赖数据。新仓库即使技术内容很多，也可能因“尚未活跃、尚未被使用”而竞争力不足。

申请前至少应具备：

- 公开且可克隆的干净仓库。
- 明确的开源许可证与素材授权边界。
- 英文主 README、中文 README、架构说明和快速开始。
- 至少一个可运行的最小示例，以及发发示例角色。
- 可验证的 CI、测试和连续几个正式 Release。
- 真实的使用数据：星标、下载量、外部 Issue/PR、第三方角色包或引用项目。
- 一份具体的 Codex/API 使用计划，而不是泛泛地说“用 AI 开发”。

如果尚无显著数据，也可以申请，但必须具体解释它为什么对桌宠创作、虚拟角色工具链或 Electron 生态有独特价值。成功率仍会低于已经有明确采用度的项目。

## 3. 当前情况

### 3.1 已具备的基础

- 技术栈：Electron、Vite、JavaScript。
- 代码规模：约 58 个源代码文件、8,600 余行源代码、44 个测试文件。
- 桌面能力：透明窗口、托盘、全局快捷键、安全存储。
- 交互能力：拖拽、抛掷物理、动作注册与调度。
- 平台能力：Bilibili 适配器；已加入 YouTube Live Chat 第一版适配器与平台工厂。
- AI 能力：LLM、TTS、ASR、本地记忆与向量相关模块。
- 媒体能力：视频角色渲染。
- 动作生产能力：44 项动作注册（43 项启用、1 项停用保留），并已建立以 ImageGen 为视觉来源、由脚本完成抽帧、透明视频编码、绑定和 QA 的完整角色包 Skill。
- 扩展基础：Live2D 导入骨架、VTube Studio 接口。
- 本地 Git 历史中已有 4 个提交，并保留了多个可运行历史版本。

### 3.2 当前不能直接公开推送的原因

1. 旧 Git 历史约 1.95 GB，包含多个超过 GitHub 单文件上限的安装包、ZIP 和 `app.asar`。
2. 源码中仍有大量 `fafa`、`发发`、固定标题、固定应用 ID、固定资源路径和固定人格配置。
3. 动作注册仍主要写在代码中；`pet.json` 草案、清单校验器和资源解析边界已经建立，但尚未把 Electron/renderer 全链路切换到动态角色包。
4. `Live2DPetRenderer` 目前仍属于未完成接口，迁移配置也会关闭部分 Live2D/VTS 选项。
5. `package.json` 当前为 `UNLICENSED`，还没有明确区分框架代码许可和角色素材授权。
6. 历史安装包不能被当作对应源码的可复现构建，除非之后能恢复并验证各版本的精确源代码快照。

因此，公开仓库应采用**干净的新 Git 历史**，只纳入源码、测试、文档和必要的小型示例资源；安装包和便携版作为 GitHub Release 附件保存。

## 4. 历史版本盘点

现有文件中共识别出 9 个历史版本：

| 顺序 | 版本 | 当前可见产物 | 备注 |
|---:|---|---|---|
| 1 | `v0.1.1` | 安装包 | 最早可识别版本 |
| 2 | `v0.1.2` | 安装包、ZIP | 可作为第二个历史快照 |
| 3 | `v0.2.1` | 安装包、便携版 | 产物较完整 |
| 4 | `v0.2.2` | 便携目录 | 需要重新压缩为发布附件 |
| 5 | `v0.2.3` | 便携目录 | 需要重新压缩为发布附件 |
| 6 | `v0.2.4` | 便携目录 | 需要重新压缩为发布附件 |
| 7 | `v0.2.5` | 便携目录 | 需要重新压缩为发布附件 |
| 8 | `v0.2.6` | 安装包、便携目录 | 安装包目前误放在 `release-v0.2.2` 下，发布前需纠正 |
| 9 | `v0.3.0` | 安装包、便携目录 | 当前最高历史版本 |

这些版本应明确标记为：

> Historical binary snapshot：该附件是项目迁移时整理出的历史二进制快照，其最初构建时间早于 GitHub 发布日。除非另有说明，不保证能够由公开仓库当前源码逐字节复现。

这样既能保留演进历史，也不会制造“每天开发出一个新版本”的错误印象。

## 5. 同日发布三个历史系列归档

在公开仓库完成最小安全整理后，同一天发布三个 GitHub Release。每个 Release 代表一个历史版本系列，而不是把每个补丁版本伪装成单独的新开发版本：

| 顺序 | Release 标题 | 建议标签 | 收纳内容 |
|---:|---|---|---|
| 1 | `Fafa v0.1 Series Archive` | `archive-v0.1` | 已验证的 `v0.1.1`、`v0.1.2` 产物 |
| 2 | `Fafa v0.2 Series Archive` | `archive-v0.2` | 已验证的 `v0.2.1` 至 `v0.2.6` 产物 |
| 3 | `Fafa v0.3 Series Archive` | `archive-v0.3` | 已验证的 `v0.3.x` 产物 |

随后不再继续发布角色硬编码的历史系列；第一个框架化正式版本从 `v0.4.0` 开始。

发布原则：

- 三个系列归档可以在同一天按顺序发布，不伪造提交、构建或发布日期。
- 附件文件名保留真实内部版本，例如 `0.2.6`，不得重新命名成不存在的 `0.2.0` 构建。
- 系列归档标签使用 `archive-*`，避免被依赖工具误认为可安装的语义化正式版本。
- 每个补丁版本都必须单独通过版本、隐私和启动检查；某个附件失败时，只从对应系列归档中移除该附件，不伪造通过结果。
- 发布日期使用真实 GitHub 发布日期；原始构建日期能确认时单独写明。
- `v0.4.0` 起，源码标签、角色包、框架版本和构建结果必须可追溯。

## 6. 每个版本的发布门禁

每个历史版本发布前必须完成以下检查：

### 6.1 版本与产物

- 启动程序并确认显示的版本号。
- 核对安装包、便携版、目录名和内部元数据。
- 便携目录统一打包为 `PetWeaver-Fafa-vX.Y.Z-windows-portable.zip`。
- 安装包统一保留为清晰可识别的文件名。
- 生成 `SHA256SUMS.txt`，记录所有发布附件的 SHA-256。

### 6.2 隐私与安全

- 扫描 API Key、Token、Cookie、账号、绝对路径和个人信息。
- 删除日志、缓存、崩溃转储、测试对话、记忆数据库和用户配置。
- 检查 Electron 本地存储、Cookies、Local Storage、IndexedDB 和缓存目录。
- 不把真实服务凭据写入示例配置。
- 对安装包做基础恶意软件检查，并记录误报情况。

### 6.3 可用性

- 在断网状态下完成一次启动与退出测试。
- 测试拖拽、托盘菜单、关闭/退出和基础动作。
- 确认缺少外部 AI 服务时不会崩溃，并给出可理解的提示。
- 确认安装版和便携版不会互相污染配置。

### 6.4 权利与说明

- 明确代码许可。
- 明确“发发”形象、视频、声音、字体、音乐和其他素材是否允许再分发。
- 无法确认权利的素材不得放入公开源码仓库或 Release。
- Release Notes 必须说明这是历史快照，并列出已知限制。

## 7. GitHub Release 说明模板

```markdown
# Fafa Desktop Pet vX.Y.Z

这是 Desktop-pet-fafa 迁移到 PetWeaver 过程中的历史二进制快照。

## 状态

- 原始构建时间：YYYY-MM-DD（如能确认）
- GitHub 发布时间：YYYY-MM-DD
- 平台：Windows
- 类型：历史版本，不代表当天新开发版本

## 附件

- `PetWeaver-Fafa-vX.Y.Z-windows-setup.exe`
- `PetWeaver-Fafa-vX.Y.Z-windows-portable.zip`
- `SHA256SUMS.txt`

## 已验证

- [ ] 可启动
- [ ] 可正常退出
- [ ] 拖拽与基础动作正常
- [ ] 离线启动不会崩溃
- [ ] 已移除用户数据和凭据

## 已知限制

- 本版本为历史二进制快照。
- 除非另有说明，不保证能够由公开仓库当前源码逐字节复现。
- 某些在线、语音或直播功能可能需要用户自行配置第三方服务。
```

## 8. 新仓库的整理方式

### 8.1 纳入仓库

- `src/` 源代码。
- `test/` 或现有测试目录。
- 构建脚本、包管理锁文件和必要配置。
- README、贡献指南、架构说明、角色包规范。
- 不涉及授权风险的小型示例资源。
- `pets/fafa` 示例角色，但仅包含确认可公开分发的素材。

### 8.2 不纳入 Git 历史

- `node_modules/`。
- `dist/`、`release-*`、`test-build-*`。
- `.exe`、`.zip`、`.asar` 等构建产物。
- 日志、缓存、用户数据、Cookies、数据库和本机配置。
- 未确认授权的角色、字体、音乐、视频和声音素材。

旧 `.git` 不应直接推送。新公开仓库从经过清理的源码快照开始，历史安装包通过 GitHub Releases 补齐。

## 9. PetWeaver 最终架构

建议逐步整理为：

```text
petweaver/
├─ packages/
│  ├─ core/                 # 生命周期、事件、动作、状态、配置
│  ├─ runtime-electron/     # 窗口、托盘、快捷键、安全存储
│  ├─ renderer-video/       # 视频角色渲染
│  ├─ renderer-live2d/      # Live2D 渲染实现
│  ├─ adapter-bilibili/     # Bilibili 直播适配
│  ├─ provider-llm/         # LLM 提供方接口与实现
│  ├─ provider-tts/         # TTS 提供方接口与实现
│  └─ provider-asr/         # ASR 提供方接口与实现
├─ pets/
│  └─ fafa/                 # 发发角色包与授权说明
├─ examples/
│  └─ minimal-pet/          # 最小可运行示例
├─ tools/
│  └─ create-petweaver/     # 创建新桌宠项目的脚手架
├─ docs/
│  ├─ pet-manifest.md
│  ├─ renderer-api.md
│  ├─ provider-api.md
│  └─ migration-from-fafa.md
└─ README.md
```

### 9.1 角色清单

角色包应使用声明式清单，例如：

```json
{
  "schemaVersion": 1,
  "id": "fafa",
  "name": "发发",
  "version": "1.0.0",
  "renderer": {
    "type": "video",
    "entry": "assets/idle.webm"
  },
  "actions": "actions.json",
  "persona": "persona.md",
  "permissions": ["audio", "network"]
}
```

底层框架不再通过搜索 `fafa` 或固定文件名来识别角色，而是读取角色清单。

### 9.2 稳定扩展边界

至少需要定义四类稳定接口：

1. `PetPackage`：角色资料、动作、资源和人格。
2. `Renderer`：视频、Live2D，以及未来其他渲染方式。
3. `PlatformAdapter`：Bilibili 等事件来源。
4. `AIProvider`：LLM、TTS、ASR 和记忆实现。

核心只负责编排生命周期与事件，不直接依赖某个角色或某个第三方服务。

### 9.3 角色动作生成不是单一 ImageGen 调用

PetWeaver 的创作者 Skill 必须覆盖项目真实动作注册表，而不是只生成少量通用动作：

- 当前完整注册表有 44 项，其中 43 项默认启用。
- ImageGen 只负责产生统一角色参考图和动作姿势图。
- 动作分镜来自项目现有语义定义，每项动作生成八个连续姿势。
- 后续必须执行布局检查、抠色、128×128 帧归一化、预览、384×416/60 fps VP9 透明视频编码和全量清单校验。
- 任何启用动作缺失时，校验默认失败；只有角色清单明确声明且语义合理的回退才能放行。

默认美术方向为像素风，但角色包合同不绑定某个画风。

## 10. 分阶段目标

### 阶段 0：公开前整理

- 完成文件、凭据和素材权利扫描。
- 建立干净的新 Git 历史。
- 补充许可证、README 和安全配置说明。
- 准备并验证首个历史 Release。

完成标准：源码仓库不含大型二进制、个人数据和未授权素材，可被陌生开发者克隆。

### 阶段 1：去角色硬编码

- 建立 `pet.json` 清单格式。
- 把标题、应用 ID、默认人格、动作和资源路径移出底层代码。
- 将发发整理为 `pets/fafa`。

完成标准：仅替换角色包即可启动另一个最小桌宠，不需要修改核心源码。

### 阶段 2：模块边界

- 抽离 core、Electron runtime、renderer、adapter 和 provider。
- 为公共接口补充类型声明、错误约定与生命周期文档。
- 保留兼容层，避免一次性重写现有功能。

完成标准：核心测试不需要启动完整 Electron，也不需要真实网络服务。

### 阶段 3：创作者工具

- 提供 `create-petweaver` 脚手架。
- 提供最小角色示例和清单校验器。
- 提供开发模式、资源热重载和错误提示。

完成标准：新用户可在 15 分钟内从模板创建、启动并修改一个自己的桌宠。

### 阶段 4：渲染与平台生态

- 完成 Live2D renderer，或明确将其标记为实验功能。
- 稳定视频 renderer。
- 把 Bilibili 改为可选适配器；YouTube 已完成第一版 Live Chat 轮询接入，后续补充 OAuth/自动发现与低延迟 `streamList` 实现。

完成标准：任一 renderer 或 adapter 的缺失都不会阻止最小桌宠运行。

### 阶段 5：可维护发布

- CI 自动执行 lint、测试、构建和清单校验。
- 使用 SemVer、Changelog 和迁移说明。
- 新版本可从标签可复现构建。
- 发布框架包、示例角色和桌面应用时各自拥有清晰版本。

完成标准：标签、源码、构建结果和发布附件之间可追溯。

## 11. 近期执行顺序

1. 保存本文，冻结迁移目标和历史版本顺序。
2. 对 `fafa` 做完整的隐私、凭据、素材和大文件扫描。
3. 为新公开仓库准备源码白名单，建立干净 Git 历史。
4. 确定代码许可证与发发素材的授权方式。
5. 分别建立 `v0.1`、`v0.2`、`v0.3` 系列归档候选目录，对其中每个附件执行门禁。
6. 同一天依次发布 `archive-v0.1`、`archive-v0.2`、`archive-v0.3` 三个历史系列归档。
7. 从 `v0.4.0` 开始只发布框架化版本：角色清单、可替换角色包和通用适配器成为正式兼容契约。
8. 同步推进角色清单、去硬编码、创作者 Skill，以及 Bilibili/YouTube 直播适配器边界。

## 11.1 2026-09-16 已开始的 v0.4 工作

- 新增 `skills/petweaver-pixel-pet`：从实际注册表准备 44 项任务，默认像素风，ImageGen 只负责视觉源图。
- 已完成抽帧、Pillow 画布处理、Remotion 自带精简 FFmpeg 的 VP9-alpha 编码兼容和 ffprobe 校验脚本。
- 冒烟测试确认：仅有 1 个视频时会报告缺少 42 个启用动作；补齐 43 个启用动作后元数据校验通过。
- 新增 `src/main/core/pet-package.js`，建立角色清单路径、动作覆盖、表情资源和回退循环校验。
- 新增 `src/main/pet-package-service.js`、内置发发角色包和受限 `pet-resource://` 协议；设置页可以扫描、选择外部角色包，renderer 会即时切换动作视频与表情资源，LLM 会使用当前角色包的人格提示词。
- 新增直播适配器工厂与 YouTube Live Chat 适配器；API Key 与公开配置分离保存。
- 新增 `docs/pet-packages.md` 与 `docs/live-adapters.md`，开始补齐第三方创作者使用说明。

## 12. 暂不做的事情

- 不把 1.95 GB 的旧 Git 历史强行推送到 GitHub。
- 不为了“看起来持续更新”伪造提交日期或构建日期。
- 不在没有验证的情况下批量上传全部安装包。
- 不把未完成的 Live2D 能力描述为已经可用。
- 不在素材权利不清晰时给框架代码与角色资源套用同一个许可证。
- 不进行一次性大重写；优先建立配置边界，再逐模块抽离。

## 13. 最终目标与验收定义

PetWeaver 的最终目标不是把 `Desktop-pet-fafa` 换一个名字，而是把它变成一个真正可复用的桌宠创作底座。

项目达到第一阶段成熟状态时，应同时满足：

- 陌生开发者能够克隆仓库并按文档启动示例。
- 无需修改核心代码即可创建第二个角色。
- 角色包可以声明资源、动作、人格和所需权限。
- 渲染器、平台适配器和 AI 提供方是可选模块。
- 发发只是官方示例之一，不再是框架的隐式全局设定。
- 仓库不含凭据、个人数据、大型构建产物和来源不明素材。
- 每个正式版本都能从对应标签构建，并有测试、校验和校验和记录。
- README 能让使用者清楚区分“框架”“角色包”“桌面发行版”三者。

在此之前，所有公开版本都应诚实标记为迁移期或历史快照。

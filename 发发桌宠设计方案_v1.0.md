# 《发发》AI 桌面宠物 · 设计方案

> 版本：v1.0（待评审）
> 日期：2026-08-12
> 状态：设计定稿，进入实施排期

---

## 1. 项目概述

### 1.1 产品定义

| 项目 | 内容 |
|---|---|
| 名称 | 发发（Fafa） |
| 形态 | Windows 桌面宠物（独立 exe 安装程序） |
| 触发词 | `发发` / `fafa` / `fuafua`（三者等价，均可触发指令） |
| 安装包 | electron-builder 打包，NSIS 安装程序，安装图标名 `fafa` |
| 视觉资产 | **fafa pet**（自制的透明背景正面形象图；参考图为斯普拉遁风墨鱼少女插画 `fafa.png`） |

### 1.2 核心能力一览

1. 桌面陪伴：待机动作、随机小动作、情绪变化、随机台词、整点彩蛋
2. 直播互动：B站 弹幕触发指令、点歌、礼物动画、小游戏
3. 智能对话：接入 DeepSeek 语言模型（可开关）、TTS 配音播报
4. 直播集成：OBS 窗口捕获，桌宠直接出现在直播画面
5. 鼠标交互：拖拽、点击、抚摸、右键菜单、甩飞

### 1.3 目标用户与场景

- 场景一：主播日常直播——发发作为直播间虚拟形象，弹幕点歌/送礼/抽奖增加互动
- 场景二：个人桌面陪伴——挂机陪伴、整点报时、AI 闲聊

---

## 2. 技术架构

### 2.1 技术选型

| 模块 | 选型 | 说明 |
|---|---|---|
| 客户端框架 | Electron + JavaScript | 生态成熟，Live2D/帧动画/弹幕库齐全，打包 exe 方便 |
| 动画渲染 | PixiJS（一期帧动画） | 播放精灵图集帧动画 |
| 动画渲染 | Live2D Cubism SDK for Web（二期） | 替换渲染层，无缝升级 |
| 弹幕接入 | bilibili-live-ws（JS）/ 参考 blivedm 协议 | 房间号直连（默认），扫码登录（可选） |
| 语音合成 TTS | 阿里云百炼 API（默认） | Provider 接口预留本地 Edge-TTS / VITS 扩展 |
| 语言模型 LLM | DeepSeek API（OpenAI 兼容格式） | 可开关；无 Key 时走离线应答模式 |
| 点歌机 | AwooMusicBot（独立部署） | 多音源（网易云/QQ音乐/酷狗/Folia），HTTP/WebSocket 联动 |
| 配置存储 | electron-store / JSON 配置文件 | 本地持久化 |
| 打包 | electron-builder（NSIS） | 安装包 + 开机自启注册 |

### 2.2 架构分层

```
┌─────────────────────────────────────────────┐
│              渲染层（可插拔）                 │
│   AnimationProvider 接口                    │
│   ├─ 一期：FrameAnimator（PixiJS 帧动画）    │
│   └─ 二期：Live2DRenderer（Cubism SDK）      │
├─────────────────────────────────────────────┤
│              行为层                           │
│   动作状态机（待机/小动作/情绪/指令/礼物…）    │
│   优先级调度 + 打断规则                       │
├─────────────────────────────────────────────┤
│              交互层                           │
│   弹幕指令解析器 │ 鼠标交互 │ 小游戏          │
├─────────────────────────────────────────────┤
│              服务层                           │
│   弹幕客户端(房间号/扫码)                     │
│   TTS Provider │ LLM Provider               │
│   点歌机联动(HTTP/WS)                        │
├─────────────────────────────────────────────┤
│              Electron 壳                     │
│   透明置顶窗口 │ 托盘菜单 │ OBS 窗口捕获      │
└─────────────────────────────────────────────┘
```

### 2.3 关键设计原则

1. **动画层可插拔**：上层动作状态机只面向"动作 ID"，不关心底层是帧动画还是 Live2D。二期换 Live2D 时零改动上层逻辑。
2. **服务 Provider 化**：TTS / LLM / 点歌机均为接口化，便于替换实现和本地测试。
3. **离线可用**：不配置任何 API Key 时，发发仍可完整演示（本地固定台词 + 帧动画），方便开发调试。

---

## 3. 视觉与动画方案

### 3.1 视觉资产：fafa pet

- 现状：参考图 `fafa.png` 为斯普拉遁风墨鱼少女插画（半身、粉格纸背景、带日文梗气泡），**不可直接用作桌宠**。
- 产出物 **fafa pet**：透明背景、全身、正面的形象图（由我方人工制作/绘制）。
- 规格要求：透明背景 PNG；全身完整；动作关节（头/眼/嘴/四肢）在画面中清晰可辨；尺寸建议 1024×1024 源图。

### 3.2 一期：帧动画方案

| 项目 | 规格 |
|---|---|
| 图集单元 | 192×208 像素，透明背景 |
| 图集结构 | 8 列 × 行数不定的精灵图集（每行 = 一个动作） |
| 播放器 | PixiJS SpriteSheet + 逐帧播放 |

**动作图集生成路线（二选一，按质量决策）：**

1. **主选：hatch-pet（Codex 技能）**
   - 在 Codex 环境运行，规格统一（192×208）、位置稳定、自带确定性 QA
   - 基础图集生成后，按桌宠需求扩展动作行（唱歌/跳舞/睡觉等需另行生成）
2. **备选：ChatGPT Plus（GPT-4o 图像生成）**
   - 利用已有订阅，零额外成本
   - 约束：动作与动作之间必须连贯、角色位置不变；需抽帧人工检查帧一致性
   - **决策标准：无法保证连贯性与位置稳定时，一律回退到 hatch-pet 路线**

> 说明：一期动作均为"预设动画"；二期接入真 Live2D 后，口型同步、平滑转向等能力升级。

### 3.3 二期：真 Live2D 骨骼动画

- 工具：Live2D Cubism Editor（官方编辑器，已免费开放含商用）
- 流程：fafa pet 正面图 → 拆层（眼/嘴/发/身/四肢）→ 绑定网格骨骼 → 导出 `.model3.json` + 纹理 → Live2D SDK for Web 加载
- 动作：每个动作一个 `.motion3.json`，替换动画层接口即可，上层指令表/状态机不变
- 收益：TTS 口型同步（按音量驱动嘴部参数）、平滑转向、Live2D 表情（`.exp3.json`）

### 3.4 动作清单（38 种，先核心后扩展）

**待机（3）**

| 动作 ID | 名称 | 说明 |
|---|---|---|
| idle_breath | 呼吸待机 | 默认循环，轻微起伏 |
| idle_blink | 眨眼待机 | 待机中周期性眨眼 |
| idle_stare | 发呆待机 | 目光放空，随机触发 |

**随机小动作（12）**

| 动作 ID | 名称 | 动作 ID | 名称 |
|---|---|---|---|
| act_stretch | 伸懒腰 | act_spin | 转圈 |
| act_scratch | 挠头 | act_flip_hair | 甩头发 |
| act_yawn | 打哈欠 | act_table | 趴桌 |
| act_lookaround | 左右张望 | act_roll | 打滚 |
| act_hop | 蹦跳 | act_chin | 托腮 |
| act_rub_eye | 揉眼睛 | act_sneeze | 打喷嚏 |

**情绪（8）**

| 动作 ID | 名称 | 动作 ID | 名称 |
|---|---|---|---|
| emo_happy | 开心 | emo_shy | 害羞 |
| emo_angry | 生气 | emo_sleepy | 困倦 |
| emo_sad | 难过 | emo_excited | 兴奋 |
| emo_surprise | 惊讶 | emo_aggrieved | 委屈 |

**指令动作（10）**

| 动作 ID | 名称 | 触发方式 |
|---|---|---|
| act_sing | 唱歌 | 发发*唱歌 / 点歌联动 |
| act_dance | 跳舞 | 发发*跳舞 |
| act_sleep | 睡觉 | 发发*睡觉 |
| act_wake | 起床 | 发发*起床 |
| act_work | 打工 | 发发*打工（斯普拉遁梗） |
| act_ink | 涂地 | 发发*涂地（斯普拉遁梗） |
| act_firework | 放烟花 | 发发*放烟花（粒子特效） |
| act_intro | 自我介绍 | 发发*自我介绍 |
| act_greet | 打招呼 | 发发*你好/嗨 |
| act_joke | 讲笑话 | 发发*讲笑话 |

**礼物（3）**

| 动作 ID | 名称 | 触发方式 |
|---|---|---|
| gift_thanks | 感谢小礼物 | 收到辣条/小电视等通用礼物 |
| gift_sc | SC 感动 | 收到醒目留言 |
| gift_guard | 舰长狂喜 | 上舰/提督/总督（大动画+特效） |

**特殊（2）**

| 动作 ID | 名称 | 触发方式 |
|---|---|---|
| special_hourly | 整点彩蛋 | 每小时整点报时 |
| special_lottery | 抽奖开奖 | 弹幕抽奖开奖时刻 |

> 排期原则：待机 3 + 常用指令 8 + 情绪 4（开心/生气/难过/惊讶）为一期核心动作集，其余二期分批补齐；部分指令可复用同一动作模组（见指令表"动作复用"列）。

---

## 4. 触发与指令系统

### 4.1 弹幕接入

- **默认模式：房间号直连（匿名）**——配置直播间房间号即可连接 B站 弹幕服务器，可收到：弹幕、礼物、SC、上舰横幅
- **可选高级模式：扫码登录**——弹幕更完整、可主动回复弹幕（"发发**回复**"功能预留），登录态持久化
- 测试方式：未开播阶段可连接任意测试直播间验证弹幕流（账号情况：先测试后开播）

### 4.2 指令格式

```
发发*指令内容      （等价：fafa* / fuafua*）
点歌*歌曲名/链接   （点歌专用指令，优先级最高）
```

### 4.3 权限与防刷屏

- 权限：**全体观众可用**，不分级
- 防刷屏：**每人 30 秒冷却**（按弹幕用户 ID 计），冷却期内再次触发仅忽略
- 全局限流：点歌指令排队播放（播放中的歌优先）

### 4.4 指令表（初版）

| 弹幕指令 | 动作 | 台词/回复 | 动作复用 |
|---|---|---|---|
| 发发*唱歌 | act_sing | "来一首！" | 点歌联动 |
| 点歌*XXX | 点歌动画（短播） | "收到《XXX》！" | act_sing 模组 |
| 发发*跳舞 | act_dance | "蹦起来～" | - |
| 发发*睡觉 | act_sleep | "呼噜……" | - |
| 发发*起床 | act_wake | "早呀！" | - |
| 发发*打工 | act_work | "打工魂燃烧！" | 复用模组 |
| 发发*涂地 | act_ink | "涂满全场！" | 复用模组 |
| 发发*放烟花 | act_firework | "砰砰砰！" | - |
| 发发*自我介绍 | act_intro | 自我介绍台词 | - |
| 发发*你好/嗨 | act_greet | "你好呀～" | act_greet |
| 发发*讲笑话 | act_joke | 随机笑话 | 复用模组 |
| 发发*夸夸 | 无动作/复用 | "你最棒啦！" | 复用模组 |
| 发发*加油 | 无动作/复用 | "冲鸭！" | 复用模组 |
| 发发*报时 | 无动作/复用 | 播报当前时间 | 复用模组 |
| 发发*抽奖 | special_lottery | 进入抽奖流程 | 小游戏模块 |
| 发发*生气/开心/难过 | 对应情绪 | 情绪台词 | 情绪系统 |
| 未收录指令 | 默认待机 | 离线：固定回应；AI 模式：交 DeepSeek | - |

> 设计约定：指令可配置为"只回复不做动作"或"复用同一动作模组"，避免 38 种动作全部要求独立动画。

### 4.5 AI 自由对话（开关控制）

- 默认**关闭**：指令表优先，未收录指令走本地固定回应
- 开启后：未收录指令交 DeepSeek API，结合发发人设生成回复
- **离线应答模式**：未配置 API Key 时自动启用本地假回复，保证功能可测试

---

## 5. 互动模块

### 5.1 点歌机集成（AwooMusicBot）

- 形态：**独立部署**（Windows 免安装单文件运行），发发通过其 HTTP/WebSocket 接口联动
- 音源：网易云 / QQ音乐 / 酷狗 / Folia（多音源，符合需求）
- 规则：
  - 默认播放主播歌单（AwooMusicBot 配置默认歌单）
  - 观众 `点歌*XXX` → 直接切播观众点的歌，进入点歌队列（排队机制）
  - 发发收到联动事件：头顶气泡显示歌名 + 播放点歌短动画（act_sing 模组）
- OBS：AwooMusicBot 自带浏览器源（127.0.0.1:5555）可独立叠加，发发仅做联动层

### 5.2 礼物动画

| 礼物类型 | 动画 | 台词 |
|---|---|---|
| 通用小礼物（辣条/小电视/金瓜子等） | gift_thanks（小动作） | "谢谢老板～" |
| 醒目留言 SC | gift_sc（朗读+感动表情） | 朗读 SC 内容 |
| 上舰（舰长/提督/总督） | gift_guard（大动画+金币特效） | "感谢上舰！" |

### 5.3 鼠标交互（全部启用）

| 交互 | 行为 |
|---|---|
| 拖拽移动 | 按住发发任意拖动，松手站稳 |
| 点击互动 | 单击/双击触发随机台词或互动动作 |
| 抚摸跟随 | 鼠标划过时眯眼/蹭手/哼声 |
| 右键菜单 | 快捷操作（说话/换动作/设置/退出） |
| 甩飞/弹回 | 甩出去飞走再跑回来（整蛊趣味） |

### 5.4 小游戏（一期：弹幕抽奖）

- 流程：观众刷 `发发*抽奖` 参与 → 发发喊停开奖（`发发*开奖`）→ 中奖者获得彩带特效 + 特殊台词
- 二期候选：猜拳、答题抢答、弹幕竞猜、AI 接梗

### 5.5 情绪系统（四来源）

| 来源 | 示例 |
|---|---|
| 弹幕关键词 | 弹幕含"生气/开心/难过"等 → 对应情绪 |
| 礼物联动 | 小礼物开心 / SC 感动 / 上舰狂喜 |
| 随机切换 | 定时随机切换情绪，自然变化 |
| 鼠标联动 | 抚摸开心 / 甩飞生气 |

---

## 6. 行为状态机

### 6.1 状态定义

```
待机(idle) ──随机定时器──> 随机小动作(1~3s) ──> 回到待机
   │                              │
   ├──整点────> 整点彩蛋(报时+小动作)
   ├──情绪源──> 情绪状态(可带台词)
   ├──弹幕指令─> 指令动作
   ├──礼物────> 礼物动画
   └──鼠标────> 交互动作
```

### 6.2 优先级（高→低）

```
1. 点歌联动 / 抽奖开奖
2. 舰长级大动画
3. SC 朗读
4. 通用礼物
5. 弹幕指令动作
6. 情绪状态
7. 随机小动作
8. 待机
```

### 6.3 打断规则

- 高优先级动作可打断低优先级动作（播完回到待机）
- 待机/小动作永不打断高优先级
- 同一动作播放中重复触发：重置播放或忽略（按指令配置）

---

## 7. 人设与台词

三个候选方案（实施前选定一个，或混搭）：

| 候选 | 性格 | 示例台词 |
|---|---|---|
| A 元气墨鱼少女 | 活泼话痨，满口斯普拉遁梗 | "今天的涂地练习开始啦！""イカすでしょ？！" |
| B 慵懒毒舌 | 爱睡觉、欠揍毒舌 | "又来找我？行吧……""摸鱼多快乐啊" |
| C 温柔陪伴型 | 治愈系，适合挂机陪伴 | "辛苦啦，休息一下吧""我一直在哦" |

- 随机台词库：一期 ≥ 50 条（按场景分：待机/打招呼/情绪/整点/礼物/AI 回复前缀），二期扩充至 200+
- 台词支持"梗词库"替换（如斯普拉遁术语），营造专属感

---

## 8. 语音与 AI

### 8.1 TTS 配音播报（阿里云百炼）

- 默认 Provider：阿里云百炼（DashScope）语音合成 API（免费试用额度起步）
- 用途：念随机台词、念 SC、点歌反馈、AI 对话回复
- **TTS Provider 接口设计**（预留扩展）：

```js
interface TTSProvider {
  speak(text, options): Promise<AudioBuffer>   // 合成
  voiceList(): Promise<Voice[]>                 // 可用音色
}
// 实现：AliyunBailianTTS（默认）| EdgeTTS（本地免费备选）| VitsTTS（本地模型）
```

- 口型联动：一期帧动画播放"说话"动图循环；二期 Live2D 按音量驱动嘴部参数

### 8.2 LLM（DeepSeek）

- 接入方式：DeepSeek API（OpenAI 兼容格式），可在设置中配置 Base URL / API Key
- 人设注入：系统提示词携带发发人设（候选方案之一）+ 直播上下文
- 开关：设置面板总开关；无 Key 自动切离线应答模式

---

## 9. 直播集成（OBS）

- **接入方式：窗口捕获**——发发为透明置顶窗口，OBS 添加"窗口捕获"选中发发窗口，透明背景保留
- 注意事项：
  - Windows 下透明窗口捕获需确认 DWM 合成正常（Win10/11 默认满足）
  - 若个别显卡驱动下透明失效，备选：浏览器源（发发本地 URL 页面）
- 弹幕可视化：弹幕/礼物事件以气泡形式出现在发发头顶，随窗口一起被捕获

---

## 10. 设置与打包

### 10.1 托盘菜单（设置入口）

- 打开设置 / 切换人设 / 测试 TTS / 重启弹幕连接 / 退出

### 10.2 设置面板配置项

| 分类 | 配置项 |
|---|---|
| 弹幕 | 房间号、扫码登录、测试模式 |
| 动画 | 动画源（帧动画/Live2D）、缩放、帧率 |
| 语音 | TTS 音色、语速、音量 |
| AI | DeepSeek 开关、API Key、人设选择 |
| 点歌 | AwooMusicBot 地址、联动开关 |
| 窗口 | 置顶、开机自启、点击穿透、透明度 |
| 指令 | 指令表编辑（新增/修改/删除） |

### 10.3 窗口行为（默认全开）

- 置顶显示（可在设置关闭）
- 开机自启（可在设置关闭）
- 点击穿透可切换（穿透时发发不挡操作，仅鼠标事件可交互）

### 10.4 打包

- electron-builder + NSIS 安装包，安装图标 `fafa`
- 开机自启：安装时可选注册启动项

---

## 11. 风险与合规

| 风险 | 影响 | 对策 |
|---|---|---|
| 网易云/QQ音乐接口失效 | 点歌不可用 | 多音源冗余（AwooMusicBot 已支持 4 源）；定期维护；准备本地歌单兜底 |
| 歌曲版权问题 | 直播侵权风险 | 仅用于个人直播场景；点歌限流；不建议商用 |
| B站 接口变动 | 弹幕断开 | 跟随开源库更新；断线自动重连 |
| 弹幕账号风险 | 匿名直连更安全 | 匿名模式为主，扫码为可选 |
| LLM 费用 | 持续成本 | AI 对话默认关闭；设置用量提醒 |
| 硬盘缓存 | 点歌下载占空间 | AwooMusicBot 缓存清理策略（定期清理/限大小） |
| 打包体积 | 安装包偏大 | Electron 按需裁剪，压缩资源 |

---

## 12. 开源依赖清单

| 库/工具 | 用途 |
|---|---|
| Electron | 桌面应用壳 |
| electron-builder | exe 打包 |
| PixiJS | 帧动画渲染 |
| Live2D Cubism SDK for Web（二期） | Live2D 渲染 |
| bilibili-live-ws（JS） | B站 弹幕 WebSocket 客户端（参考 blivedm 协议） |
| 阿里云百炼 DashScope API | TTS 语音合成 |
| DeepSeek API | 语言模型对话 |
| AwooMusicBot | 弹幕点歌机（独立部署，多音源） |
| electron-store | 配置持久化 |
| hatch-pet（Codex 技能） | 帧动画图集生成（主选） |
| GPT-4o 图像生成（ChatGPT Plus） | 帧动画图集生成（备选） |
| Live2D Cubism Editor（二期） | 建模与动作制作 |

---

## 13. 开发路线图

| 阶段 | 内容 | 里程碑 |
|---|---|---|
| Phase 0 技术验证 | Electron 骨架、透明置顶窗口、PixiJS 帧动画播放器、房间号弹幕读取、托盘菜单 | 可运行的最小壳 |
| Phase 1 MVP | 动画层接口、待机/核心动作集、指令表解析、头顶气泡、离线应答 | 桌宠本体可用 |
| Phase 2 互动 | AwooMusicBot 联动、礼物动画、鼠标交互、情绪系统、弹幕抽奖 | 直播互动闭环 |
| Phase 3 智能化 | TTS 播报、DeepSeek 对话开关、整点彩蛋、台词库扩充 | 完整功能版 |
| Phase 4 打磨发布 | 动作补齐、性能优化、electron-builder 打包、开机自启、图标 | v1.0 发布 |
| Phase 5 二期升级 | Live2D 建模替换渲染层、新增小游戏 | Live2D 版 |

---

## 14. 附录：待办事项

- [ ] 制作 fafa pet 正面形象图（透明背景、全身）
- [ ] 选定发发人设（A/B/C 三选一）
- [ ] 用 hatch-pet 生成基础动作图集，验证帧一致性
- [ ] 注册阿里云百炼账号，申请 TTS 试用额度
- [ ] 申请 DeepSeek API Key（可选）
- [ ] 部署 AwooMusicBot，确认房间号连接与点歌流程
- [ ] 搭建 Electron 项目骨架（Phase 0）
